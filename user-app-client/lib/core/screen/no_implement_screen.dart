import 'package:flutter/material.dart';

class NoImplementScreen extends StatelessWidget {
  const NoImplementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
